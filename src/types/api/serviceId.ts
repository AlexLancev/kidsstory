export type ServiceIdType = {
  map(arg0: (el: ServiceIdType) => import("react/jsx-runtime").JSX.Element): unknown;
  _id: string;
  icon: string;
  imagePromo: string;
  imageBg: string;
  imagePreview: string;
  title: string;
  description: string;
};
